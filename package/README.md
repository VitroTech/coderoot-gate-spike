# @vitrotech/gate-spike

Throwaway package for a trusted-publishing experiment. It exists to prove that a release
runner can publish to npm as a trusted publisher, with no stored token. Do not depend on it.
